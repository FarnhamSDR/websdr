#!upstart

description "Upstart script for 143 MHz rtl_tcp"
 
start on starting websdr
stop on shutdown or stopped websdr
respawn

script
    echo "Starting 143 MHz rtl_tcp.."
    exec rtl_tcp -d 0 -f 143000000 -s 2048000 -g 40 -a 127.0.0.1 -p 5903
end script
